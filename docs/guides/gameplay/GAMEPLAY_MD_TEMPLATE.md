# Gameplay MD Template

本文档定义项目级 `gameplay.md` 的默认输出结构。

生成或更新 `gameplay.md` 时，应先根据 `QUESTION_FRAMEWORK.md` 收集必答问题的答案，再把答案整理到下面结构中。

```md
# Gameplay

## 1. 游戏定位

## 2. 3C

### 2.1 Character

- 玩家主体是什么。
- 写明目标模型文件、期望朝向、尺寸、动画需求和是否需要绑定 `modelId`。

### 2.2 Control

### 2.3 Camera

## 3. 资源链路

### 3.1 资源列表

记录第一版需要的模型、贴图、音频、UI 图片和其他资源。模板不默认绑定具体角色模型；如需通过 `scene.json` 使用模型，推荐先在 `scene.assets` 增加 asset，再在 `scene.nodes` 中增加引用该 asset 的 instance node。

### 3.2 可收集资源来源

### 3.3 资源存放与容量

### 3.4 资源转换链条

### 3.5 转换节点、执行者与钱的产生

### 3.6 HUD 显示资源

### 3.7 资源数量规则

### 3.8 资源交互触发规则

## 4. 核心循环

### 4.1 阶段总览

### 4.2 阶段详情

### 4.3 升级列表

### 4.4 升级消耗、触发方式与完成效果

### 4.5 阶段完成规则与阶段级变化

### 4.6 失败条件

### 4.7 初版完整游玩路径

## 5. 假设项
```
